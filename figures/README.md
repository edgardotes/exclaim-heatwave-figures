# Reference figures

This directory contains the reference outputs reproduced by `python run_all_figures.py`.

The manuscript-facing filenames are deliberately preserved because they match the figure files used in the paper workflow.
